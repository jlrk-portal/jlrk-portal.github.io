# Supabase CLI 로컬 상태 (링크 시 자동 생성, 커밋 대상 아님)
supabase/.temp/
supabase/.branches/
.env

# Claude Code 개인 설정 (권한 허용 목록 등)
.claude/settings.local.json

# 원본 정산 엑셀 (실데이터 — 공개 repo 커밋 금지)
Engine Oil Package 정산(old)/
Pick up & Delivery(Old)/
.DS_Store
